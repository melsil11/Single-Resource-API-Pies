<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Pies</title>

        <!-- link in bootstrap -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <!-- link in our css -->
        <link rel="stylesheet" href="/styles.css" />
    </head>
    <body>
        <nav class="navbar navbar-expand-md navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="/pies">Pies</a>
                {% if loggedIn %}
                    <a class="nav-link text-light" href="/pies/mine">Hello {{username}}</a>
                {% endif %}
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
                data-bs-target="#navbarNav" 
                aria-controls="navbarNav" aria-expanded="false" 
                aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <a class="nav-link text-light" href="/pies"> All the Pies</a>
                    {% if loggedIn%}
                    <a class="nav-link text-light" href="/pies/new">New Pies</a>
                    <a class="nav-link text-light" href="/users/logout">Log Out</a>
                    {% else %}
                    <a class="nav-link text-light" href="/users/signup">Sign Up</a>
                    <a class="nav-link text-light" href="/users/login">Log In</a>
                    {% endif %}
                </div>
            </div>
        </nav>
        <main class="container-md">
            {% block content %}
                default content
            {% endblock %}
        </main>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    </body>
</html>